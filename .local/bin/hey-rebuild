#!/usr/bin/env bash

if [[ $EUID -ne 0 ]]; then
   echo "Relaunching with sudo..."
   exec sudo -E "${BASH_SOURCE[0]}" "$@"
fi

SYSTEM_TYPE=$1
shift

sudo nixos-rebuild --flake ~/.config/nixos/"$SYSTEM_TYPE"#$(hostname) "$@"
